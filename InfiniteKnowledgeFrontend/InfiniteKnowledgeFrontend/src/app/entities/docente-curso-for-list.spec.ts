import { DocenteCursoForList } from './docente-curso-for-list';

describe('DocenteCursoForList', () => {
  it('should create an instance', () => {
    expect(new DocenteCursoForList()).toBeTruthy();
  });
});
